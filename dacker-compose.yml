version: '3.9'

services:
  mysql:

    image: mysql:5.7

    container_name: restaurant_mysql

    restart: always

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: restaurant_db

    ports:
      - "3306:3306"